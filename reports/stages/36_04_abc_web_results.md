# Stage 36 A/B/C 启停对照及网页交付

2026-09-09：A/B/C 结果已加入 http://192.168.100.162:8036/ 的 ADC 对页面。

- A：一次连续 900 秒，分析时切成九个 100 秒段。
- B：九次 100 秒，仅 STOP/START。
- C：九次 100 秒，每次重写相同板载时钟、重载同一固件、RFDC 初始化/MTS。

19 次采集、2700 秒有效数据，3,375,000,000 包；完整性与独立复算全部 PASS。
队列结束时间 2026-09-08 13:22:15 CST，总耗时约 89.37 分钟，停止采流且 DAC 静音。
队列：GB10 `/var/lib/t510/measurements/stage36-abc-20260908-queue/`。

## 两张新图

1. 完整复可见度：每组有效 900 秒，支持每点 100 ms/1 s；按每组首段起点
   对齐，保留实际段间空隙。B/C 各有八处断点，切换阶梯线也不跨空隙连接。
2. 九个 100 秒段的归一化相关与相位：每组九点，先按有效频谱帧数平均复数
   和自功率，再计算 `100*abs(mean(V))/sqrt(mean(Pa)*mean(Pb))`。

沿用 ADC 对与频率多选，以及幅度/相位图例联动。旧三组重启比较和历史科学图保留。
每张新图右侧包含计算公式、符号说明、来源和解释边界。

## 边界

B/C STOP 返回至下一次 START 请求目标间隔为 90 秒；真实正式数据起止包含检查、
启动保护窗等额外时间，不能把空隙假定为精确 90 秒。每次启动最短保护窗约 5.2 秒，
未记录其中暂态。顺序为 A→B→C，不能排除自然漂移/温度的顺序混杂；C 同时改变
多个初始化步骤，不能只归因于 MTS。5% 相位着色门限不是显著性检验。

## 验证

- `t510_stage36_abc_web_verify.py`：A/B/C 的 100 ms 与 1 s 均验证点数、九段统计、
  每组文件身份、实际空隙，并与封存分段复算及底层复数数组独立核对，PASS。
- 162 上 Chromium：16 张科学图公式/符号/双栏布局检查 PASS；新图图例隐藏与
  恢复幅度/相位 PASS；B/C 时间曲线各八个空隙标记 PASS。已检查实际截图。
- GB10 evidence：`/var/lib/t510/measurements/stage36-abc-api-20260909.json`、
  `stage36-abc-layout-20260909`、`stage36-abc-legend-20260909.json`、
  `stage36-abc-timeline-20260909.json` 及截图。
