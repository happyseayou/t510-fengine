# Stage 36：背景训练参数与有效期研究

## 范围与顺序

先比较既有数据的训练时长/频率平均宽度，再进行全新固定状态有效期验证。
跨RFDC reset的新旧模板对照属于下一阶段，不混入固定状态采集，也不在本轮隐式Reset。
真实弱共同信号恢复仍是最终目标；减少偏置不等于减小中心化噪声或Allan方差。

## 既有数据参数筛选

训练只用DSA实验A0前1/5/10/30/60秒，各自比较1/3/5/9/17 bin的复数邻域均值。
频带边缘截短邻域，不绕回、不填零；不改变FFT/PFB或原始频率分辨率。
候选共25个，另有不扣除对照。主评分预定为28对、bin3072–3328中归一化复残差模的中位数，
取A2和A4两个评分的算术均值。使用对应检验段的功率归一化。
先将参数选择写盘并验SHA，再打开A6摘要作确认。A6此前已在其他分析中被观察，不能称全新盲测。
所有训练数据块和全频摘要验SHA；60秒训练复均值与先前独立摘要一致。

| 候选 | A2/A4开发评分（%） |
|---|---:|
| 60秒，1bin | 0.2454 |
| 30秒，1bin | 0.2672 |
| 10秒，1bin | 0.3049 |
| 60秒，3bin | 0.3055 |
| 不扣除 | 0.3306 |

选择60秒、逐bin模板。A6确认：中位归一化残差从不扣除0.3340%到0.2585%；
第90百分位从5.753%到0.6249%。这是整体描述统计，不是所有ADC对都变好，更不是显著性检验。
更长训练是否更好尚未测试，相邻bin也不能默认统计独立。
证据：`build/background-grid-20260911/{selection.json,results.json,selected_template.npz}`。
脚本：`scripts/stage-36/t510_stage36_background_grid.py`。

## 固定状态新采集

用户确认已改为八路独立50Ω且TG关闭。新接线与此前ADC0/2功分器条件分开记录。
模拟DSA沿用ADC0/2=20dB、其余0dB；不在正式窗口中调整。
current core/bitstream、320MS/s、中心200MHz、onboard TCXO、数字尺度和MTS保持固定。

队列 `stage36-background-life-20260911`：预检 → 1860秒连续复可见度 → 全数据验签与独立数值验证
→ 分钟级全频分析与固定/因果更新策略对照 → 安全停流、DAC静音、封存。
首60秒为新模板，后30分钟为检验；完整28对4096bin原生100ms和派生1s保留。
每个60秒检验段比较不扣除、固定首分钟模板、1/5/10/20分钟更新策略。
每次更新只使用已结束的60秒参考窗，不包含正在检验的目标窗，不使用未来数据。
这里所有输入为参考终端，离线滚动更新不能直接移植为对天文目标数据扣均值。

连续窗口不Reset、不MTS、不CONFIGURE、不改DSA、不清错误。采流完整性、FIR/FFT和RFDC错误必须零；
任何失败停流保留现场，不自动重试。沿用Stage35/36已验证的采集、监控、数值验证和封存实现。
脚本 `scripts/stage-36/t510_stage36_background_lifetime_queue.py`。
测试覆盖复数平滑边界、零残差、因果窗不泄漏未来、1860秒合法时长、禁止配置/时钟写入。

有效期由残差随模板年龄及各ADC对的变化描述，不凭单条曲线指定更新周期。
只有与已知弱信号的误差预算相结合，才能最终规定合格阈值和正式标定频率。
跨reset阶段在本次有效期结果核验后，固定参数再比较旧/新模板；不重复调参寻找有利结果。

## 提交身份

- unit：`t510-stage36-background-life-20260911.service`
- Invocation：`ae4d9e35cd1f43b1adc998cfe6798559`
- 快照：`/home/astrolab/.cache/t510/stage36-background-life-20260911/repo/`
- 队列证据：`/var/lib/t510/measurements/stage36-background-life-20260911-queue/`
- 提交脚本：`build/background-life-20260911/submit.sh`
- 预检读回：DSA符合预期，停止采流，DAC静音，八路RFDC错误位零。提交记录不代表采集已完成。

## 固定状态队列完成

用户询问后恢复检查：队列completed、verification_status=PASS、error=null，耗时约34.1分钟。
1860秒采集完成，正式板端/接收端丢包、序号和sample0断点、解析及接口错误均零；
停流后的八路RFDC错误位均零。最终状态复查：停止采流，八路DAC全部关闭。
queue manifest验SHA，背景分析和停流probe与封存文件SHA一致。

对每个后续60秒检验段，先取28对、bin3072–3328归一化复残差模中位数，再取30段的中位数：

| 策略 | 30段评分中位数 % | 最后一分钟评分 % |
|---|---:|---:|
| 不扣除 | 0.3706 | 0.3451 |
| 固定首分钟模板 | 0.2414 | 0.2521 |
| 每1分钟更新 | 0.1872 | 0.1571 |
| 每5分钟更新 | 0.2280 | 0.2271 |
| 每10分钟更新 | 0.2485 | 0.2698 |
| 每20分钟更新 | 0.2435 | 0.2698 |

固定模板在该评分下到最后一分钟仍优于不扣除；每分钟更新在本次比较中评分最低。
这不是逐对逐bin的合格判定，不能将0.1872%当成全机统一噪声底或直接据此规定正式更新周期。
输入均为参考终端，更新使用此前已完成的60秒窗；未证明接天线时可用相同方式更新。
本次没有Reset，跨Reset转移仍未检验。
本地摘要 `build/background-life-20260911/background_lifetime.json`，队列状态同目录保存。

### 逐ADC对检查

对每一对，先取每分钟科学频段257个bin归一化复残差模中位数，再取30分钟中位数。
固定模板较不扣除改善26/28对，例外ADC2/7（0.284→0.305%）和ADC3/7（0.213→0.216%）。
每分钟更新较不扣除改善28/28对，也较固定模板改善28/28对，均只指上述汇总指标，不代表每一分钟、每个频点都改善。
ADC0/1：8.019→0.297→0.210%；ADC4/5：6.321→0.296→0.262%（不扣除/固定/每分钟更新）。
固定模板在ADC0/1与ADC4/5的30个分钟均优于不扣除；弱相关对存在个别分钟变差。
细表 `build/background-life-20260911/pair_summary.json`。不据此把一分钟当成已验证最优正式周期。
