# Stage 36：900 秒复可见度补采与三组叠加

2026-09-08：重启前、重启后两次补采均完成，独立复算及完整性检查 PASS。
每次 900 秒、28 对、4096 通道；全频原生 100 ms 与派生 1 s 产品保留。
每次 1,125,000,000 包，正式窗口丢包及序号断点为零。
第二次结束后回读确认停止采流、八路 DAC 禁用。

## 数据与身份

GB10 `/var/lib/t510/measurements/` 下：

- 原始组：`stage36-science-20260906-1852-pairs-xcorr-scan-900s`
- 重启前补采：`stage36-repeat1-20260907-xcorr-900s`
- 重启后补采：`stage36-repeat2-20260907-xcorr-900s`

两次补采的 queue 目录保存完整性结果、验签、独立数值复算及安全结束证据。
第二组 manifest SHA256：`35902b516b37a9c0127a7912480e726c2270311cc44b125bd2f9a309148000b8`。
固件 core `0x00010036`，bitstream SHA256
`e00c586a1d862d7c7af113361832a30093334493e49f943e3dd22bf44f950665`。

第二组之前发生断电、SD 卡故障与换卡、同版本组件重新部署；因此这些结果不是
只改变“重启”一个因素的因果实验。物理输入按已确认的八路独立 50 Ω、DAC 静音、
板载参考执行。完整冷启动 CONFIGURE 的 LMK/PL 顺序问题仍需永久修复，
本次使用已验证的 clock-preserving 流程恢复。

## 页面

http://192.168.100.162:8036/ 的 ADC 对长相关图叠加三组。
各组横轴均为相对自身起点的时间，图例名称含组名、ADC 对及频率；
点击同一图例同时控制幅度和两类相位点。100 ms 与 1 s 均支持。
短原始谱、短平均谱和 Allan 图仍使用原评测的数据来源。

后端缓存键包含组身份，响应提供各组自己的 scan ID 与 manifest SHA。
现行配置位于 `/opt/t510-stage36-explorer/current/app_config.json`，
历史数据及原始配置不改写。

## 验证

GB10 evidence：`stage36-three-scan-api-20260908.json`、
`stage36-three-scan-browser-20260908`、`stage36-three-scan-legend-20260908.json`。
三组各自 100 ms/1 s 点数为 9000/900；抽查 ADC0–ADC1 bin3073 的幅度和相位与
底层复数数组一致。浏览器检查单 ADC/ADC 对共 14 张图的公式及符号说明、
双栏布局、页面无横向溢出；长相关图例隐藏/恢复幅度与相位测试通过。

示例：ADC0–ADC1，120.078125 MHz，100 ms 点的幅度均值依次为
8.214654、4.551825、1.730580 count²。这是 `mean(abs(V))`，不是
`abs(mean(V))`，不能仅凭幅度下降宣称科学性能改善。
