# Stage 36：共同单音短采

2026-09-11，接续 [DSA 准备](36_17_common_tone_dsa_preparation.md)。用户确认 TG 已开启，按先前设置记录：
SSA3032X Plus，130.078125 MHz，Zero Span，−20 dBm；功分器连接 ADC0/2，其他六路独立 50 Ω。
请求电平不等于已测量的 ADC 连接器电平。ADC0/2 DSA 为 20 dB，其他路 0 dB。

## 队列与边界

一次提交 10 s 检查 → 通过后 60 s 稳态采集 → 独立产品校验与分析 → 停流、DAC 静音。
保存全频复可见度的 100 ms 和 1 s 产品，以及每段相关窗口后相邻的原始谱见证。
此见证检查 F-engine IQ16 余量，不能代替原始 ADC 码值检查，也不覆盖相关窗口全部采样。

检查实际 DSA、数字身份、丢包/序号、FIR/FFT 和 RFDC 错误；原始谱最大 I/Q 绝对值须小于 26213。
两路主峰须在 bin 3201 ±2，且共同峰处功率高于附近频带中位数至少 10 dB。
相关系数不设通过阈值，以免将低相干的真实现象过滤掉。
报告两路功率比、复平均归一化相关、相位及每 100 ms 波动。
单频结果不证明宽带时延对齐，也不等于原独立噪声底改善。

TG 开启期间仅 STOP/START，不执行 CONFIGURE、Reset、时钟恢复或 MTS；任一门禁失败停止。
外部 TG 由用户控制，队列不能关闭它。

## 启动问题与证据

预检发现 RFDC 粘滞标志：ADC0/1/4/5/6/7 为 `0x34000000`，ADC2 为 `0x74000400`，ADC3 为 `0x7c400400`。
先保存原读数，在停流状态仅清除一次已观察到的标志，立即及 5 s 后八路均为零。
这只建立新的错误观测窗口，不证明先前错误的来源，也不保证后续不会复发。
新监测掩码 `0xFCFF0FFF` 包含高位模拟错误；未沿用旧 QMC 实验遗漏高位的掩码。
队列内不重复清标志。

首次队列 `stage36-tg-capture-20260911` 在接收器创建请求时失败：metadata 要求字符串，新增布尔/数组字段违反接口。
未开始采流，安全清理无错误。保留其全部证据；修复为接口层 JSON 字符串编码，队列证据仍保留原类型。
补充接口类型回归，并通过原始谱满量程、已知相位/相关、缺失源停队列等自检。

第二次 `stage36-tg-capture-20260911-r2` 也在创建请求时失败：追加频点后超过 focus 列表的 32 点上限。
已按接收器 `validate_request` 核对请求约束，新频点优先、focus 总数限制为 32；完整 4096 频点的 100 ms 产品不变。
补充数量、去重、目标频点保留检查，自检通过。前两次均未进入 START，失败证据保留。

再次提交 `t510-stage36-tg-capture-20260911-r3.service`，Invocation ID：
`b47a60de47954bd68f71630bafa2b855`。本文件是提交记录，不代表采集通过。

交还控制权时：r3 第一段接收状态 completed，12,500,000 包发布及消费，ring_drops=0，
completed_block_mask=65535；队列仍 running，第一段完整门禁/封存尚未确认结束，60 s 阶段 pending。
确认实际接收健康后停止轮询，由同一队列自动完成余下门禁和接续。

- GB10 证据：`/var/lib/t510/measurements/stage36-tg-capture-20260911-r3-queue/`
- 执行快照：`/home/astrolab/.cache/t510/stage36-tg-capture-20260911-r3/repo/`
- 快照逐文件 SHA：上述 repo 的同级 `source-manifest.json`，包含工作区未提交成果。
- 本地准备、清标志和提交脚本：`build/tg-capture-20260911/`
- 板端清标志持久记录：`/home/xilinx/t510-stage36-tg-monitor/interrupt-journals/`
- 实现：`scripts/stage-36/t510_stage36_tg_{queue,analyze,monitor,clear_errors,selftest}.py`

后续先检查本队列最终状态再继续；不得将共同源结果与八路独立负载的噪声结果直接等同。

## 最终结果（用户询问完成后核验）

r3 队列 completed，verification_status=PASS，两段独立数值验证 PASS。
10 s / 60 s 分别发布并消费 12,500,000 / 75,000,000 包，正式完整性检查均通过，
丢包、序号和 sample0 断点均为零。两段结束后八路 RFDC 错误位均零。

| 测量 | 10 s | 60 s |
|---|---:|---:|
| 共同主峰 bin | 3201 | 3201 |
| 归一化复平均幅度 | 99.999303% | 99.999275% |
| ADC0×conj(ADC2) 相位 | −0.201951° | −0.199142° |
| ADC0/ADC2 功率比 | 0.369067 dB | 0.337188 dB |
| 100 ms 相位相对全段均值的 P5/P95 | −0.007552° / +0.006703° | −0.003828° / +0.003826° |

共同峰对应 130.078125 MHz。60 s 相邻原始谱见证 ADC0/2 最大 I/Q 绝对值为 5482/5264 count，
低于本实验余量门限；不把它解释成全窗口原始 ADC 满量程证据。
当前状态回读 streaming=false，DAC enable_mask=0。外部 TG 仍由用户控制。

结果支持当前配置下两路对已知强共同单音有稳定相干响应；没有检验重启后重复性、宽带时延、
弱信号灵敏度，也没有解决之前独立负载下相关底随初始化变化的问题。
