# Stage 35：T510 射电天文噪声科学研究方案

> 状态：Stage 35 设计基线，供新对话直接接续
> 日期：2026-08-30
> 范围：承接已结题的 Stage 34f，转入科学表征；本文件本身不启动采集、构建或板卡操作

## 0. 一句话目标

不再用“通过/失败”评价 T510，而是把它当作一台尚未绝对定标的射电天文接收机：用生产版 F-engine 的完整输出，逐一测量 8 路 ADC、每个观测频段内全部 4096 个频点的绝对计数、短时变化、积分规律和时间记忆，并明确这些量在 2 s、4 s、15 s 和最长 30 s 天文积分下会怎样影响观测。

第一阶段只研究每一路的自相关谱和时间噪声。ADC 之间的互相关不是被否定，而是独立放到后续“干涉测量阶段”，避免两个问题互相掩盖。

Stage 35 的代码、服务、证据目录和报告文件统一使用 `stage35` 标识；不得继续复用 34f 的任务名、诊断 bitstream、临时 API 或证据目录。

---

## 1. 为什么从 Stage 34f 转向这一方案

Stage 34f 的多轮工程实验已经把问题边界压到了很靠前的位置。根据现有记录，可以继承下列工作认识：

- 8 路 ADC 使用彼此独立的 50 Ω 终端，DAC 已物理断开。
- 去掉外部 White Rabbit 板卡提供的 10 MHz 和 PPS、改用板内时钟后，所关心的时间结构仍然出现。因此，外部 WR 不是该现象存在的必要条件。
- D0/D1 等后级数字路径实验与 D2/RFDC 前端观测共同把关注范围收窄到 ADC、RFDC、采样时钟以及未被现有遥测覆盖的前端模拟条件；这仍是“范围”，不是已证明的唯一器件根因。
- OCB1、WR、DAC 等方向已经反复做过控制实验。除非新数据提出一个可证伪的新假设，否则不再无目的重跑这些组合。
- 既有 D2 数据不是生产 F-engine 的完整频谱：它是少量指定频点的主机侧稀疏 DFT 诊断，并带固定 stride。D2 的 100 ms 桶也只是充分统计量，不是逐个 320 MS/s 样点。它适合定位，不适合作为最终的射电天文性能描述。
- 肉眼看一条 15 s、100 ms 间隔的 RMS 曲线“很随机”，并不能证明没有时间记忆。弱自相关、低频功率过量、积分不按白噪声缩放以及桶内平均造成的隐藏，都需要统计量才能看见。

因此后续问题改为：

1. 每一路、每个频点的自相关功率随时间究竟怎样变化？
2. 这种变化在 2 s、4 s、15 s、30 s 积分下能平均掉多少？
3. 它是窄带、宽带、固定杂散附近特有，还是随整个带通共同变化？
4. 在没有绝对温标时，怎样用严谨的射电天文语言和绝对仪器单位表达？
5. 完成单路表征后，28 条通道对的复可见度中还剩下什么相关噪声？

这是一项科学表征，不再是工程门禁，也不输出“8 路中几路通过”。

---

## 2. 必须固定的术语和三个时间尺度

后续代码、报告和对话必须区分以下三个时间量。

### 2.1 原生桶 / dump：`Δt_dump`

这是落盘统计量的最短时间分辨率，不是最终天文积分时间。

- 首选目标：**10 ms**，因为用户明确希望尽可能保留短时结构，计算量和存储量不是首要限制。
- 实现验证时同时测试 10 ms、20 ms、50 ms 和现有 100 ms。
- 如果 10 ms 在生产全频谱流量下可以无丢失稳定运行，正式数据就采用 10 ms；20 ms 只作为重采样对照。
- 只有实测证明 10 ms 会造成无法消除的丢包、写盘或实时累加问题，才退到 20 ms；再失败才考虑 50/100 ms，并在元数据和报告中明确写出原因。
- 桶边界必须由 F-engine `sample0`/谱帧序号定义，不能依赖主机到包时间。

在 320 MS/s、4096 点频谱下，完整谱帧间隔约为 12.8 μs。10 ms 并不总是整数个谱帧，因此每桶会自然包含 781 或 782 帧；20 ms 则包含 1562 或 1563 帧。必须保存真实计数并据此归一化，不能假定每桶样本数完全相同。

10 ms 和 20 ms 都能把主要科学积分写成整数桶：

| 科学积分 | 10 ms 桶数 | 20 ms 桶数 |
|---:|---:|---:|
| 2 s | 200 | 100 |
| 4 s | 400 | 200 |
| 15 s | 1500 | 750 |
| 30 s | 3000 | 1500 |

### 2.2 科学积分：`τ`

这是射电天文结果所对应的积分时间。

- 主结果固定给出：**2 s、4 s、15 s**。
- 30 s 是硬上限，只作为最长对照。
- 为理解尺度变化，允许附加 `Δt_dump`、2/5 倍桶、0.1 s、0.2 s、0.5 s、1 s、8 s 等点。
- Allan、相关函数、时间功率谱和任何积分曲线都不得用 `τ > 30 s` 得出结论。
- 时间相关图的主视野为 0–15 s；15–30 s 只作为补充，不向更长时间外推。

### 2.3 一次扫描的观测长度：`T_obs`

扫描长度只是为了得到许多个 2/4/15/30 s 样本，不是把整段扫描积分成一个数。

建议基线数据采用 **3 次相互独立的 15 min 扫描**。每次扫描可提供 30 个不重叠的 30 s 段、60 个 15 s 段以及更多 2/4 s 段；三次重复用于估计结果是否可复现。这里没有数小时 soak，也没有大于 30 s 的科学积分。

---

## 3. 仪器配置与第一组观测条件

### 3.1 第一阶段固定物理状态

- ADC0–ADC7：8 个彼此独立的 50 Ω 终端，全程不选“重点通道”。
- DAC：保持物理断开。
- 外部 10 MHz 与 PPS：均断开；使用 T510 板内时钟。
- White Rabbit：第一阶段不需要配置，也不接入信号链。
- 空调：在稳定开启的条件下采集基线，但不人为要求数小时预热。
- 同步记录：室温、负载附近温度、板上可读温度/电压、扫描开始结束时间以及任何人工操作。

没有 WR 时，绝对 UTC 不是本阶段的核心量；相对时间以硬件 `sample0` 为准。到了真正的双天线干涉阶段，共同时钟、PPS、延迟和绝对时间会重新成为必要条件，届时必须单独标定，不能直接把本阶段结论外推过去。

### 3.2 F-engine 固定配置

首组数据使用已经验证的生产链，而不是 D2 诊断链：

- 320 MS/s 复基带；
- 4096 通道、8-tap PFB；
- IQ16 F-engine 输出；
- `SPEC_ONLY`；
- 记录确切 bitstream SHA、`CORE_VERSION`、PFB profile/CRC、`fft_shift`、RFDC 配置和中心频率；
- 当前 PFB 通道间隔为 78.125 kHz，等效噪声带宽 ENBW 约为 70.8796 kHz（0.9072586 bin）。

对任一调谐中心 `f_center`，频点映射必须使用：

```text
k_signed = k                       , 0 <= k < 2048
k_signed = k - 4096                , 2048 <= k < 4096
f_RF(k)  = f_center + k_signed × 78.125 kHz
```

若沿用 `f_center = 1020 MHz`，则完整带宽约为 860.000000–1179.921875 MHz。以后增加新的中心频率时，每个 `tuning_id` 都完整分析 4096 个 bin，不能只复用几个诊断频点。

已知 RFADC 固定项 480/960/1440 MHz 必须在频谱中标注。以 1020 MHz 为中心时，960 MHz 位于本观测带内。原始结果中不得删除该 bin 或邻近 bin；面向连续谱/谱线解释时可以另做一份显式 mask，但“全 bin 原始结果”始终保留。固定杂散和宽带慢变化是两个不同问题，不能因为看见一个固定峰就把全部时间噪声归因于时间交织。

---

## 4. 单位：先诚实使用仪器单位，再逐级进入 K 和 Jy

### 4.1 目前可以直接报告的量

射电天文语言和绝对物理定标不是一回事。在没有冷热负载或已知噪声源前，可以用天文领域的统计概念，但单位必须诚实：

| 数据层 | 推荐名称 | 当前单位 | 不应写成 |
|---|---|---|---|
| TIME 路径复样点 | RFDC/TIME 输入样点 | ADU（I、Q 各自） | 天线温度、Jy |
| F-engine 复输出 `X_a(t,k)` | 通道化电压 / F-engine 复计数 | IQ16 count | 原始 ADC ADU |
| `P_aa=<|X_a|²>` | 自相关谱 / 总功率谱 | count²/PFB channel | dBm、K、Jy |
| `P_aa/ENBW` | 仪器功率谱密度 | count²/Hz | W/Hz，除非已知模拟增益 |
| `σ_P(τ)` | 绝对辐射计散布 | count²/channel | 只有百分比的稳定度 |
| 功率自协方差 | 时间记忆强度 | count⁴ | 无单位相关系数的替代品 |
| Allan variance / deviation | 积分尺度稳定性 | count⁴ / count² | 频率标准的分数频率 ADEV |

报告可以同时给出 `ΔP/P`、归一化 ACF、拟合斜率等无量纲量，但它们只能是辅助面板，不能替代 ADU、count 或 count² 的绝对纵轴。

### 4.2 “原始 ADU”必须怎样展示

F-engine IQ16 count 不能未经证明就叫作 ADC ADU。为了满足对 ADU 变化的观察，每次 SPEC 扫描前后各做一次独立的 `TIME_ONLY` 测量：

- 在线按 10/20 ms 统计每个 ADC 的 I/Q 均值、标准差、复 RMS、min/max、削顶计数和直方图；
- 至少覆盖一个完整 30 s 段，报告重点画出连续 15 s，使用全部原生桶；
- 可额外保存 50–200 ms 的连续 TIME 原始包，供逐样点直方图、量化码和短时相关检查；
- 明确标注这些是 RFDC 解调/抽取后的 TIME 路径 ADU，还是更靠近 ADC converter 的码流；不能模糊写成“3.84 GS/s 原始码”；
- 320 MS/s 下当前产品不能同时输出完整 TIME 与 SPEC，所以两者属于相邻的控制观测，而不是同一时刻的同步样本。报告必须写清这一限制。

### 4.3 从 count 到 K/Jy 的定标阶梯

当前一个室温 50 Ω 负载只能给出一个工作点。即使它接近 290 K，也不能单独解出线缆损耗、失配、前端增益和接收机自身噪声，因此现在不能宣称 `T_sys`、K/count、Jy/count 或 SEFD。

后续分三步定标：

1. **数字比例定标**：用位精确 PFB 模型和已知数字激励，追踪 RFDC/TIME ADU 到 F-engine IQ16 count 的 FFT、窗函数、舍入和 `fft_shift` 比例；验证 Parseval 关系。
2. **温度定标**：在 ADC 输入端使用两个已知等效温度的匹配负载，或一个已定标的切换噪声二极管；计入线缆/衰减器损耗，以 Y-factor 求接收机噪声温度和 K/count。
3. **天文通量定标**：接天线后做已知射电源的 on/off 或漂移扫描，结合天线增益、有效面积和波束，得到 Jy/count、K/Jy 和 SEFD。

完成第 2 步前，报告标题使用“未定标自相关谱”“仪器带通”“辐射计散布”，不使用貌似物理但无法溯源的 dBm、K 或 Jy。

---

## 5. 需要新增的采集能力

现有 `spec-stability` 只支持少量选定频点，桶宽只接受 100/1000 ms。它不能满足本研究。应在已经验证过的 Rust SPEC 接收路径上新增一个**全带宽自相关累加器**，而不是再开一个互相争抢 100 GbE 流量的接收进程。

### 5.1 输入范围

- 接收每一个通过协议校验的生产 F-engine SPEC 包；
- 覆盖 ADC0–ADC7 和全部 4096 个 bin；
- 16 个 256-bin block 组成一幅完整频谱；
- 使用谱帧序号/`sample0` 检查乱序、重复、缺块和 gap；
- 缺失数据永远不能填零；保存每个桶、每个 block 的有效帧数和曝光时间。

科学分析优先使用完整频谱帧。如果为吞吐量采用 block-local 累加，则每个 block 必须有独立 `N_valid`，跨频率比较和重采样必须按真实曝光加权。

### 5.2 每个桶的可合并统计量

累加过程使用 float64 或整数加宽，落盘前也不得在累加器中使用低精度求和。建议保存：

- `N_valid`；
- `mean_I`、`mean_Q`；
- `mean_P = mean(I²+Q²)`；
- `M2_P`（功率二阶中心矩，可与其他桶精确合并）；
- I/Q 饱和或削顶计数；
- bucket 起止 `sample0`、包数、缺块/重复/乱序计数；
- 主机和板上环境遥测的时间索引。

这些量足以离线重组 2/4/15/30 s 积分，并计算功率方差、spectral kurtosis 和 Allan 统计。若只保存已经归一化的一个百分比，将无法恢复所需科学量，因此禁止这样做。

### 5.3 分层落盘，保留细节又控制体积

生产 F-engine 复输出的有效负载约为 81.92 Gbit/s（约 10.24 GB/s），长期保存全部复谱不现实。采用在线充分统计与短原始片段相结合：

- **A 层，10 ms 全频段主数据**：全部 8×4096 的 `mean_P`（float64）和精确计数。约 26.2 MB/s，15 min 约 23.6 GB；若实测只能采用 20 ms，则约 13.1 MB/s、11.8 GB/15 min。
- **B 层，100 ms 全频段矩数据**：`mean_I`、`mean_Q`、`M2_P`、削顶等可合并量；避免重复保存已经在 A 层存在的功率字段。约数 GB/15 min，按实际字段核算。
- **C 层，短原始数据**：短时完整 F-engine 复谱片段和 50–200 ms TIME 原始包，用于验证累加、PFB 短记忆和 ADU 分布。

使用 HDF5 或 Zarr 的分块数据集，时间维为第一检索维度之一；统计长表另存 Parquet。压缩可用，但容量预算必须按“随机噪声几乎不可压缩”的最坏情况准备。每个文件保存 SHA-256。

### 5.4 100 ms 桶问题的专项验证

不再争论 100 ms 本身“好或坏”，而是做可重复比较：

1. 用同一段 packet replay 同时生成 10/20/50/100 ms 统计；
2. 把 10 ms 结果分别离线合并成 20/50/100 ms，与对应原生累加结果逐项比较；
3. 用不同窗口起点形成 offset 版本，检查结论是否依赖桶边界；
4. 对合成白噪声、1/f 噪声、慢增益漂移、脉冲和相干单音分别验证；
5. 所有统计计算使用全部有效桶，绘图中的像素压缩不得参与计算。

只有上述实现一致后，才采正式科学数据。

### 5.5 数据存放边界

- 采集机 `192.168.100.162` 是生产 UDP 的唯一消费者，也是 Stage 35 原始数据、分桶统计、HDF5/Zarr/Parquet 和短 PCAP/TIME 片段的唯一落盘位置。
- 本开发机及本仓库只保存源代码、测试、配置模板、观测 manifest、数据集索引、SHA-256、摘要表和最终报告；不复制长期原始 UDP、PCAP、TIME、HDF5 或 Zarr 数据。
- 报告中的数据链接必须指向采集机上的归档身份或可搬运的数据集 manifest，不能假装仓库中存在原始数据。
- 若为了单元测试需要极小 packet fixture，应使用合成数据或经过明确裁剪、无设备敏感信息的固定 fixture，并把体积保持在代码审查可接受范围。

---

## 6. 观测流程

### S0：方法与实现验证，不接管板上长期任务

1. 在现有 Rust receiver 中加入全带宽自相关累加器和分块 writer。
2. 为 10/20/50/100 ms 加入按 `sample0` 分桶的能力。
3. 用合成数据和已保存 packet replay 验证：数值精度、桶合并、乱序、重复、丢包、sample0 wrap、文件恢复。
4. 用位精确/等价 8-tap PFB 白噪声模拟，得到相邻谱帧因 PFB 重叠而产生的预期短时相关基线。
5. 做 60 s SPEC 烟雾采集，只检查包连续性、磁盘吞吐和元数据是否完整。这是数据完整性检查，不是科学“通过门禁”。

### S1：TIME-only ADU 控制观测

在与正式谱观测相同的 RFDC 配置和物理接线下：

1. 每个 ADC 同时统计至少 30 s TIME 数据；
2. 保存全 30 s 的 10/20 ms ADU 统计，展示连续 15 s 的全部点；
3. 保存一个短连续原始样点片段；
4. 给出每路 I、Q 的均值、RMS、标准差、直方图、码型占用、min/max 和削顶计数；
5. 不把某一路作为代表，也不把 8 路压缩成一个计数结论。

### S2：50 Ω 终端的生产 F-engine 基线

建议队列为：

```text
TIME pre 30 s
SPEC scan A 15 min
TIME post 30 s

TIME pre 30 s
SPEC scan B 15 min
TIME post 30 s

TIME pre 30 s
SPEC scan C 15 min
TIME post 30 s
```

三次扫描保持同一硬件配置，但分别保存独立 `scan_id`。扫描之间的短间隔用于关闭/重开观测文件和确认元数据，不更换输入、不调 ADC 参数。

如果该队列被用户明确作为长任务提交，则严格遵守仓库规则：一次性提交完整队列，确认健康启动后立即交还控制权；阶段间自动继续，只在失败时停下并保留证据。本文档不授权当前对话自动提交它。

### S3：数字比例和温度定标

S2 报告完成后再设计。优先顺序是数字比例追踪，然后是两温负载/定标噪声源。没有新增标定硬件前，不重复 S2 期待它自行产生 K/count。

### S4：单天线天文观测

具备 K/count 后，接天线做 off-source、on-source 或漂移扫描。仍使用 2/4/15/30 s 积分，并报告全频段动态谱、系统温度、带通稳定性和积分灵敏度。RFI mask 只用于派生科学产品，原始全 bin 结果保留。

### S5：双天线/阵列干涉扩展

完成单路时间噪声表征后才进入，详见第 10 节。

---

## 7. 每一路、每个频点必须计算的数值

最小索引是：

```text
scan_id × tuning_id × adc_id(0..7) × global_bin(0..4095)
```

对这个索引的每一个组合，都必须保留具体值，不能只给汇总比例。

### 7.1 频谱与绝对水平

- `global_bin`、`signed_bin`、精确 RF 频率、通道宽度和 ENBW；
- 有效谱帧数和有效曝光；
- `mean_I`、`mean_Q`，单位 F-engine count；
- `RMS_X = sqrt(mean(I²+Q²))`，单位 count；
- `mean_P`，单位 count²/channel；
- `mean_P/ENBW`，单位 count²/Hz；
- 中位数、5/16/84/95 百分位以及绝对 min/max；
- 该 bin 是否为 DC、PFB 边缘、已知固定杂散或其邻近区，只作标签，不删除数据。

### 7.2 在各积分时间下的变化

对 `τ = 2, 4, 15, 30 s`，至少计算：

- 每个不重叠积分块的绝对 `P_τ` 时间序列；
- `mean(P_τ)`、`std(P_τ)`、MAD 和置信区间，单位均保留为 count²/channel；
- 实测绝对散布 `σ_meas(τ)`；
- 白噪声/PFB 模型预期的绝对散布 `σ_white(τ)`，与实测曲线画在同一绝对纵轴上；
- `σ_meas/σ_white` 和 `ΔP/P` 可附加，但不能成为唯一结果；
- `σ(τ)` 的局部缩放斜率及其拟合区间，不把一条斜率自动判为成功或失败。

白噪声参考不能只套一个未经验证的 `1/sqrt(Bτ)` 常数。应同时给出：

1. 使用 70.8796 kHz ENBW 和明确统计假设的理论值；
2. 白噪声通过实际 8-tap PFB/定点缩放后的模拟值；
3. 从观测数据短滞后协方差估计的有效独立样本数。

三者不一致本身就是结果。

### 7.3 Allan 分析

这里分析的是自相关功率稳定性，不是振荡器的分数频率稳定性。对每个 ADC、每个 bin 都输出：

```text
AVAR_P(τ) = 1/2 × mean[(P̄_{i+1}(τ) - P̄_i(τ))²]   [count⁴]
ADEV_P(τ) = sqrt(AVAR_P(τ))                         [count²]
```

- 主曲线从原生桶延伸到 15 s；可补充到 30 s，禁止超过 30 s；
- 同时保存 overlapping 和 non-overlapping 版本，报告中注明两者的有效自由度；
- 画绝对 ADEV（count²）为主，fractional ADEV 为辅；
- 若 30 s 内出现极小点，可称为“本观测范围内的 Allan 最小值”，不能外推成长期 Allan time；
- 原始数据和去温度回归后的结果并列，原始结果始终在前，不允许隐藏去趋势。

### 7.4 时间相关和时间功率谱

对功率序列 `P(t,k)`：

- 自协方差 `C_P(lag)`，单位 count⁴，主 lag 范围 0–15 s；
- ACF 作为同图的无量纲辅助量；
- temporal PSD，单位 count⁴/Hz，保留原始频率轴；
- 低频过量、窄周期项、缓慢共同起伏均以数值和谱图描述，不贴“失败”标签；
- 对温度、电压遥测做带时间戳的协方差/回归，但只称“关联”，不直接称因果；
- 提供未去趋势、减常数、温度回归三种明确标记的版本，禁止默认高通或多项式去趋势。

### 7.5 分布与异常统计

- `P` 的直方图、QQ 图或等价分布图；
- spectral kurtosis 及其绝对样本数；
- I/Q 均值是否存在相干偏置；
- 削顶、突发、缺包与真实高功率事件分开标注；
- 置信区间使用保留时间结构的 block bootstrap，block 长度由实测 ACF/PFB 短记忆确定并写入元数据。

---

## 8. 图表和单文件 HTML 报告的硬性要求

最终科学报告仍输出为**一个自包含 HTML 文件**：不依赖网络 CDN，不把 CSS/JavaScript/关键图片散落成外部文件。大体积 HDF5/Zarr/Parquet 是权威数据档案，不算报告页面的组成文件；HTML 中记录它们的相对路径、大小和 SHA-256。

### 8.1 总览不能取代逐项结果

总览页可以解释现象，但禁止出现：

- “8 路有 N 路通过”；
- 红绿灯、pass/fail、工程门限排名；
- 只显示百分比或只显示 ADC 均值；
- 只挑 ADC0、ADC3 或几个“代表 bin”。

### 8.2 8 路 ADC 的平等展示

ADC0–ADC7 各有结构完全相同的页签/章节，每一路至少包含：

1. TIME-only ADU：15 s 连续原生桶的 I/Q 均值和 RMS，以及 30 s 概览、直方图、削顶统计；
2. 4096-bin 平均自相关带通，纵轴为 count²/channel，另给 count²/Hz；
3. 全 4096-bin 动态谱瀑布图，时间轴采用原生桶；
4. `frequency × τ` 的绝对 `σ_P` 图；
5. `frequency × τ` 的 ADEV 图；
6. `frequency × lag` 的自协方差/ACF 图；
7. 每个已知固定杂散及邻近 bin 的放大图，同时保留全带结果；
8. 任意 bin 选择器：选择后展示该 bin 的全部 15 s 原生桶点、2/4/15/30 s 序列、Allan、ACF、temporal PSD 和分布图。

热图的每一列必须对应一个真实 bin，不能先把频率平均成少数大频段。浏览器为显示长时间概览而做像素降采样时，必须：

- 明确标注“仅渲染降采样”；
- 使用 min/max envelope，避免尖峰被平均消失；
- 所有数值和统计仍用完整数据；
- 任意选中 15 s 细节窗必须绘制全部原生桶点，不抽点。

### 8.3 完整数值表

HTML 内嵌可检索表，并提供浏览器内导出，至少包含：

```text
scan_id, tuning_id, adc_id, global_bin, signed_bin, rf_hz,
n_valid, exposure_s, mean_i_count, mean_q_count, rms_x_count,
mean_power_count2, power_density_count2_per_hz,
std_power_2s, std_power_4s, std_power_15s, std_power_30s,
avar_2s, avar_4s, avar_15s, avar_30s,
adev_2s, adev_4s, adev_15s, adev_30s,
autocov_lag_values, acf_lag_values, temporal_psd_summary,
spectral_kurtosis, clip_count, data_quality_flags
```

`data_quality_flags` 只描述事实，例如缺包、固定杂散、DC、边缘 bin；它不是科学合格判定。

---

## 9. 如何区分“看起来随机”和真正的时间记忆

对每个 ADC/bin，都按下列顺序展示，避免结论被单张曲线左右：

1. 15 s 原生桶绝对时间序列；
2. 相同数据在 2/4/15 s 下的绝对积分序列；
3. 测得的 `σ_P(τ)` 与白噪声绝对参考；
4. 0–15 s 自协方差和 ACF；
5. temporal PSD 的低频形状和窄周期项；
6. Allan deviation 到 15 s，补充到 30 s；
7. 三次 15 min 扫描之间的复现性；
8. 与室温、板温、电压遥测的同步关系。

如果线条肉眼杂乱，但 `C_P(lag)` 在若干 lag 非零、PSD 有低频过量，或 `σ_P(τ)` 明显偏离白噪声绝对参考，就可以严谨地说“存在可测时间记忆”。反之，如果这些量与白噪声/PFB 模型一致，则应报告一致范围和不确定度，而不是宣布“通过”。

另需分开两个时间区间：

- **微秒级 PFB 记忆**：8-tap PFB 的窗口重叠会让相邻谱帧天然相关。用短原始复谱或在线 lag 累加器测全部 ADC/bin，并与位精确白噪声模型比较。
- **10 ms–15 s 慢记忆**：由原生桶时间序列研究，是本阶段最关心的天文积分问题。

不能把预期的 PFB 短记忆误判成模拟前端慢漂，也不能用 100 ms 平均把慢记忆隐藏掉。

---

## 10. 后续干涉测量阶段：28 条基线，仍不做“通过数”

第一阶段结束后，再对所有 `a < b` 的 28 对 ADC 计算生产 F-engine 复可见度：

```text
V_ab(t,k) = mean[X_a(t,k) × conj(X_b(t,k))]
```

对每条基线、每个 bin、每个 2/4/15/30 s 积分，输出：

- `Re(V)`、`Im(V)`，单位 count²；
- 幅度和相位，但零可见度附近以 Re/Im 为主，因为幅度有正偏、相位没有稳定物理意义；
- Re/Im 的绝对散布、Allan variance/deviation、时间自协方差和 temporal PSD；
- 复相干度仅作辅助，不能替代 count²；
- 全频带延迟谱和相位斜率；
- 每条基线独立的全 bin 图表和数值表，不输出“28 条中 N 条合格”。

物理解释分三层：

1. **8 个独立 50 Ω 负载**：理想天空可见度为零，用来量仪器内部相关底；
2. **一个已知公共噪声源经合适功分器送入多路**：用于相对增益、相位和延迟定标；
3. **两副或多副天线**：需要共同频率参考、PPS/绝对时间、几何延迟、fringe stopping、天线位置和天文源模型，才是真正阵列可见度。

因此当前 50 Ω 实验可以回答“仪器会不会制造伪相关”，但不能单独证明一套天文阵列的相位稳定性。

互相关数据体积会显著增加：28×4096 个复数/桶在 10 ms 下约为 183.5 MB/s、在 20 ms 下约为 91.8 MB/s（complex float64）。进入本阶段前，应单独决定保存全部原生桶复可见度，还是在线保留 2/4/15/30 s 可合并统计；不得悄悄只选若干基线或 bin。

---

## 11. 可复现性与元数据清单

每次 TIME/SPEC 扫描必须冻结并写入同一 manifest：

- bitstream 路径与 SHA-256、`CORE_VERSION`；
- PFB profile、系数 CRC、tap 数、FFT 长度、`fft_shift`；
- ADC sample rate、decimation、复基带率、中心频率；
- UDP payload version、product ID、接收程序 git commit；
- `scan_id`、`tuning_id`、UTC 起止、首末 `sample0`；
- 外部 10 MHz/PPS 的实际连接状态和时钟源；
- ADC0–ADC7 的物理端接、线缆/转接头、DAC 断开状态；
- 50 Ω 负载型号、标称阻抗、可测温度；
- 室温、板温、电压、风扇/空调状态；
- 每个 block 的包数、重复、乱序、gap、丢失曝光；
- dump 宽度、每桶实际谱帧数、所有分析 `τ`；
- 原始/统计数据文件大小、SHA-256 和分析代码 commit；
- 所有 mask、去趋势、温度回归和 bootstrap 参数。

任何人工删除点、频点屏蔽或时间区间裁剪都必须进入 manifest，且原始数据不改写。

---

## 12. 本研究明确不做什么

- 不再把科学结果压缩为 pass/fail、门禁比例或“几路合格”。
- 不优先 ADC0/ADC3；8 路拥有完全相同的采集、统计和图表。
- 不只看 D2 的几个诊断频点；正式结果来自完整生产 F-engine。
- 不把 F-engine count、D2 主机 DFT count 和 TIME/ADC ADU 混成同一个单位。
- 不用 `τ > 30 s` 的积分、Allan 点或相关 lag 支撑结论。
- 不把长扫描误称为长积分。
- 不在没有冷热负载/定标噪声源时报告 K、Jy、SEFD 或连接器输入 dBm。
- 不在计算阶段抽样数据点；只有长概览的渲染可以明确标注地压缩。
- 不默认去趋势、平滑或 mask，再把处理后的曲线当原始结果。
- 不无目的重跑 OCB1、WR、DAC 或旧 Stage 34f 组合。新的硬件对照必须先写出单一变量、预期区分的两个假设以及可观察结果。

---

## 13. 实施顺序与每一步的产物

| 顺序 | 工作 | 产物 | 是否需要用户现场操作 |
|---:|---|---|---|
| 1 | 检查现有 SPEC assembler，设计全频段累加器 | 数据结构、吞吐预算、实现说明 | 否 |
| 2 | 实现 10/20/50/100 ms `sample0` 分桶与 HDF5/Zarr writer | Rust 代码、单元测试 | 否 |
| 3 | 合成噪声和 packet replay 验证 | 对照报告，证明没有隐藏抽样/填零 | 否 |
| 4 | 60 s 全频段烟雾采集 | 数据完整性、吞吐和容量记录 | 需保持当前接线与板卡在线 |
| 5 | S1 TIME-only ADU 控制观测 | 8 路 ADU 统计和短原始片段 | 只需确认接线未变 |
| 6 | S2 三次 15 min SPEC 扫描 | 8×4096 全频段数据立方体 | 只需保持空调和接线稳定 |
| 7 | 离线全量分析 | Parquet 指标、图表、复现日志 | 否 |
| 8 | 生成单文件 HTML | 每路每 bin 数值与交互图 | 否 |
| 9 | 讨论定标硬件 | 热/冷负载或噪声二极管方案 | 届时再决定采购/接线 |
| 10 | 干涉阶段 | 28 基线全 bin 可见度分析 | 需要新物理信号方案 |

这张表中的“数据完整性”只判断文件能否用于科学分析，例如是否有未记录的 gap；它不对 ADC 噪声本身作工程合格判定。

---

## 14. 下一对话的直接起点

新对话不需要重述整个 Stage 34f。应先让执行者完整阅读：

1. 仓库根目录 `AGENTS.md`；
2. 本文件 `STAGE35_RADIO_ASTRONOMY_NOISE_STUDY_PLAN.md`；
3. `docs/t510_udp_payload_v2.md`；
4. `reports/stages/34_fullrate_pfb8_release.md`；
5. `rust/t510_time_rx/src/main.rs` 中现有 SPEC 接收、完整频谱组装和 `spec-stability` 实现。

然后执行的第一项任务应是：

> **只做 S0 的代码设计与实现：在现有 Rust 接收路径上加入 8×4096 全频段自相关累加器、10 ms 首选主桶（同时可测 20/50/100 ms）、按 sample0 分桶、完整 gap 元数据和分块 writer；先用 packet replay/合成数据验证，不立即提交正式板上长队列。**

开始写代码前还应给出一个简短设计审查，至少回答：

- 累加发生在完整谱组装前还是后，如何保证 16 个 block 的时间一致；
- 10 ms 桶如何处理 781/782 个谱帧，离线合并后如何与 20/50/100 ms 一致；
- 如何在不启动第二个 UDP 消费者的情况下接入；
- 每字段的数据类型、溢出边界和可合并公式；
- 15 min/三次扫描的最坏磁盘占用；
- 丢包、乱序、重复和进程中断如何落盘；
- 10 ms 与 20 ms 的 CPU/磁盘实测结果如何决定最终原生桶；
- 如何证明统计和 15 s 细节图使用了全部有效桶而不是抽点。

只有 S0 的 replay 结果和 60 s 烟雾数据完整后，才进入 S1/S2 正式观测。这样后续每一次运行都有新信息，不会回到已经反复尝试过的工程组合中打转。
