# stage 27d目前还是演示的阶段，当确认流量正常后，就要开始做生产力的收紧
# stage 27e-i TIME + SPEC， TIME预览波形，SPEC预览频谱

100MHz:
  有 PL decim2 alias 问题。
  raw 端点/带外分量被折叠进 122.88MHz。

200MHz:
  没有 PL decim2 alias。
  raw 端点/带外分量仍会直接出现在频谱边缘/低频端。
  SPEC_ONLY 200MHz 还有额外的 F-engine/output backpressure 问题。

# stage 27j TIME + SPEC， 加入PFB

# stage 28 推进200MHz频宽的全速率的 TIME_only / SPEC_only 模式
在保持100MHz模式都正常的情况下，推进200MHz模式的TIME_only / SPEC_only模式，解决现有的200MHz模式下的F-engine/output backpressure问题。
注意每次改完后都要在100MHz模式下验证（TIME_SPEC），确保没有引入新的问题。
要用vivado的mcp，等待使用阶梯式等待


# 预计stage 29开始生产力，具体要求
F-engine生产力


# stage 30
PYNQ上使用API模式

# stage 31
多板同步准备，协同API

# stage 32
修改时钟策略，改为10M的倍数，320MHz


# stage 33
原始采样率太低了，这样第一Nyquist区间内的射频输入只能到800 MHz。使用的3840MSps的采样率，DDC+12倍下抽样，这样射频输入可以到1.92GHz

# stage 33a
杂散相关的调研

# stage 34
把pfb提高到8-tap，性能评估（杂散，rfi抑制，灵敏度）

stage 34d，Allan 负责判断系统稳定时间尺度，互相关负责判断科学上是否可用。
	​

# stage 35
科学评测

# stage 36
做一些微调，重复stage 35


# stage 37
增益做成可调的

# stage 38
冷热噪声源标定

# stage 39
多板同步

# stage 40

**如果开机后一直接着天线、没有独立参考，那么目前不能仅凭这批天线数据，可靠地算出该扣多少背景。** 困难在于区分天空和仪器，而不是相减的运算。

简化来看，每个ADC对、每个频点测到的是：

\[
V_{\text{测量}}(t)=V_{\text{天空}}(t)+B_{\text{本次开机}}(t)+\epsilon(t)
\]

这里 \(B\) 是仪器复相关偏置，\(\epsilon\) 是随机测量波动。**只有左边一个观测量，不能无条件拆出右边的天空和偏置。** 把开机前60秒的天线均值当背景，会把那段真实天空相关也一起扣掉。

对当前系统，最实际的方案是增加一个**可切换的参考状态**：

1. 开机、RFDC初始化完成后，切到各路独立参考负载，估计本次背景。
2. 切回天线，使用经过验证可迁移的模板，扣除100 ms复可见度。
3. 观测前后或适当间隔再测参考，检查背景是否漂移；超出有效条件就不继续套用旧模板。

**这里仍有一道我们尚未完成的验证：参考负载测得的背景，是否适用于天线输入状态。** 加开关只是方便测参考，并不会自动解决模板迁移问题。

如果希望始终连接天线，也有更进一步的办法，例如**相位切换**：有控制地改变真实输入信号的相位，利用天空信号与某些内部偏置对切换的不同响应来分离它们。但切换位置必须在相关干扰产生之前；只在现有 F-engine 输出上乘个正负号，不能分离已经混进去的偏置。[NRAO相位切换说明](https://www.cv.nrao.edu/~athompso/Supplementary_Notes_121610.pdf)

所以，对我们下一阶段的具体建议是：**先验证“参考负载 ↔ 实际接收输入”的背景迁移，再决定如何把每次开机的参考测量自动化。** 目前不能把“开机后直接接天线、自行估计并扣除背景”作为已具备的功能。



**是的，Stage 36 可以正式认定为评估完成，并给出有明确边界的结论。你安排的 Stage 37–40 顺序也合理。**

Stage 36 已经回答了三个关键问题：

- **数字链路达到目标。**正式复评中，TIME 的 I/Q 标准差约 8.2–9.2 count，F-engine 全频中位数约 8.0–8.7 count，固件和数据完整性门禁通过。
- **仪器存在可重复观察的复相关背景，其状态可能随初始化改变。**目前没有证据把它唯一归因于 OCB2 或某个具体模块。
- **在已验证的参考负载条件下，可以估计并扣除其中的固定复偏置。**独立验证支持这种方法，但不保证所有 ADC 对、所有频点都改善；也尚未证明天线条件下的模板迁移和弱天文信号恢复。

所以可以结束 Stage 36，不必为了找出偏置的唯一物理根源而一直停留。正式结论应是“完成数字链路与参考负载评估”，不能扩大为“天文灵敏度已验证”。这些边界已经记录在[收尾报告](reports/stages/36_28_closeout.md)。

后续建议这样划分：

| 阶段 | 核心目标 | 主要验收内容 |
|---|---|---|
| **37：增益可调** | Agent 统一管理实际数字尺度 | 配置、回读、数据身份一致；验证舍入、饱和与溢出；保留 Stage 36 等效档位作为对照 |
| **38：冷热源标定** | 建立数字功率与输入噪声温度的关系 | 明确参考接入位置、损耗和有效温度；固定增益下测线性、带通、重复性及不确定度 |
| **39：多板同步** | 验证跨板时间对齐与相干性 | 公共参考、启动时刻、sample0、延迟、相位漂移，以及重启后的可重复性 |
| **40：天线实验** | 把上述能力用于真实接收链路 | 参考切换与背景迁移、幅相标定、RFI处理，再选择可检测的天文目标 |

这里有三个需要提前处理的衔接点。

**第一，Stage 37 先做手动可编程、采集期间固定的增益。**暂时不把自动增益控制混进来。改增益后，默认使旧背景模板失效；标定结果也必须绑定实际增益配置。数字增益与模拟输入量程要分清：降低后端数字增益，不能挽救已经发生的 ADC 输入削顶。

**第二，Stage 38 开始前，就要考虑未来参考负载接在哪里。**否则只标定 ADC 端，接上前端放大器、滤波器和线缆后，还需要补充整条接收链路的标定。冷热源主要解决功率／噪声温度尺度，不能替代干涉仪的相位和延迟标定。

**第三，Stage 39 要用共同输入信号验证跨板复相关。**不能只凭两块板都锁定 10 MHz、都收到 PPS 就宣布完成。AMD 的 MTS 定义包含跨 tile、跨器件的确定性延迟对齐，因此需要把实际跨板对齐作为验收对象。[AMD MTS 文档](https://docs.amd.com/r/en-US/pg269-rf-data-converter/Multi-Tile-Synchronization)

**下一步可以直接进入 Stage 37 的设计审查：先确定哪些增益可调、允许哪些档位、何时生效，以及如何记录到采集数据中。**Stage 40 的接线与参考位置可以提前规划，正式天线实验按你的顺序开展。


# 后续规划，忽略
规划：
限制adu数值到正负8192，预留一些给突发的rfi
扫一遍bandpass
