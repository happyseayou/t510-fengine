# stage 27d目前还是演示的阶段，当确认流量正常后，就要开始做生产力的收紧
# stage 27e-i TIME + SPEC， TIME预览波形，SPEC预览频谱

100MHz:
  有 PL decim2 alias 问题。
  raw 端点/带外分量被折叠进 122.88MHz。

200MHz:
  没有 PL decim2 alias。
  raw 端点/带外分量仍会直接出现在频谱边缘/低频端。
  SPEC_ONLY 200MHz 还有额外的 F-engine/output backpressure 问题。

# stage 27j TIME + SPEC， 加入PFB

# stage 28 推进200MHz频宽的全速率的 TIME_only / SPEC_only 模式
在保持100MHz模式都正常的情况下，推进200MHz模式的TIME_only / SPEC_only模式，解决现有的200MHz模式下的F-engine/output backpressure问题。
注意每次改完后都要在100MHz模式下验证（TIME_SPEC），确保没有引入新的问题。
要用vivado的mcp，等待使用阶梯式等待


# 预计stage 29开始生产力，具体要求
F-engine生产力


# stage 30
PYNQ上使用API模式

# stage 31
多板同步准备，协同API

# stage 32
修改时钟策略，改为10M的倍数，320MHz


# stage 33
原始采样率太低了，这样第一Nyquist区间内的射频输入只能到800 MHz。使用的3840MSps的采样率，DDC+12倍下抽样，这样射频输入可以到1.92GHz

# stage 33a
杂散相关的调研

# stage 34
把pfb提高到8-tap，性能评估（杂散，rfi抑制，灵敏度）

stage 34d，Allan 负责判断系统稳定时间尺度，互相关负责判断科学上是否可用。
	​

# stage 35
科学评测

# stage 36
做一些微调，重复stage 35

# stage 37

**如果开机后一直接着天线、没有独立参考，那么目前不能仅凭这批天线数据，可靠地算出该扣多少背景。** 困难在于区分天空和仪器，而不是相减的运算。

简化来看，每个ADC对、每个频点测到的是：

\[
V_{\text{测量}}(t)=V_{\text{天空}}(t)+B_{\text{本次开机}}(t)+\epsilon(t)
\]

这里 \(B\) 是仪器复相关偏置，\(\epsilon\) 是随机测量波动。**只有左边一个观测量，不能无条件拆出右边的天空和偏置。** 把开机前60秒的天线均值当背景，会把那段真实天空相关也一起扣掉。

对当前系统，最实际的方案是增加一个**可切换的参考状态**：

1. 开机、RFDC初始化完成后，切到各路独立参考负载，估计本次背景。
2. 切回天线，使用经过验证可迁移的模板，扣除100 ms复可见度。
3. 观测前后或适当间隔再测参考，检查背景是否漂移；超出有效条件就不继续套用旧模板。

**这里仍有一道我们尚未完成的验证：参考负载测得的背景，是否适用于天线输入状态。** 加开关只是方便测参考，并不会自动解决模板迁移问题。

如果希望始终连接天线，也有更进一步的办法，例如**相位切换**：有控制地改变真实输入信号的相位，利用天空信号与某些内部偏置对切换的不同响应来分离它们。但切换位置必须在相关干扰产生之前；只在现有 F-engine 输出上乘个正负号，不能分离已经混进去的偏置。[NRAO相位切换说明](https://www.cv.nrao.edu/~athompso/Supplementary_Notes_121610.pdf)

所以，对我们下一阶段的具体建议是：**先验证“参考负载 ↔ 实际接收输入”的背景迁移，再决定如何把每次开机的参考测量自动化。** 目前不能把“开机后直接接天线、自行估计并扣除背景”作为已具备的功能。

# stage 38
多板同步，把stage 32i搞过来


# 后续规划，忽略
规划：
限制adu数值到正负8192，预留一些给突发的rfi
扫一遍bandpass
